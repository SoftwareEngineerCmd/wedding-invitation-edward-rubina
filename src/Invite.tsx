import { CountDown } from "./components/CountDown";
import { Hero } from "./components/Hero";
import "./App.css";

export const Invite = () => {
  return (
    <div className="invite">
      <Hero />
      <CountDown />
      {/* ABOUT */}
      {/* GALLERY */}
      {/* SCHEDULE */}
    </div>
  );
};
